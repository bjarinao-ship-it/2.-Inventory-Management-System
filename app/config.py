class Config:
    # XAMPP MySQL Configuration (default settings)
    DB_HOST = "localhost"
    DB_USER = "root"
    DB_PASSWORD = ""  # Leave empty for XAMPP default (no password)
    DB_NAME = "inventory_db"
